---
phase: 06-foundation-hardening
plan: 00
type: plan
wave: 0
depends_on: []
autonomous: true
---

# Phase 06 — Foundation Hardening

> A wave plan to fix the weakest, most fragile points identified by the deep
> scan of 2026-08-01. Every item below carries **verified evidence** (grep or
> live gate result), a concrete fix, and a verification gate. Waves are
> ordered by impact-per-effort and dependency (persistence first: it is the
> acceptance test of the purpose clause).

## The weakness register (evidence-backed)

| # | Severity | Finding | Evidence |
|---|---|---|---|
| W-01 | CRITICAL | **Trace ledger never persists.** `TracePersistence.append()` has zero callers. `CognitiveCore.start()` opens + loads, `stop()` closes — nothing ever writes. Every restart loses the full ADR-002 ledger → replay, time machine, signal store, UER, observatory lose history. The 2803 green tests hide it (only the load path is tested). | grep: `new TracePersistence` only in `cognitive-core.ts` + `cli/uccp.ts`; both call open/loadInto/close, never append. `trace-persistence.ts:82` |
| W-02 | CRITICAL | **Organism persistence orchestration unwired.** `PersistenceProvider` (persist-by-name for the 20+ Storable stores) has zero production callers — only tests. Connectome, taste, trust, decisions, signals, patterns, genome, scientific memory, etc. are never saved in a real run. "Persistent cognition" is per-process only. | grep: `PersistenceProvider` only in `persistence.test.ts` + export in `src/index.ts` |
| W-03 | HIGH | **Wave E (learning loop + dream scan) staged, uncommitted.** Loss risk + blocks the wave queue; docs wave staged alongside. | `git status`: `AM` on learning-loop.ts, dream-scan.ts, exoskeleton.ts, neural-event-bus.ts + 16 docs |
| W-04 | HIGH | **Interface monoliths.** `stdio-server.ts` 1233 lines (whole 47-tool MCP surface in one file), `cli/index.ts` 1001 lines (documented 90–98 cc debt R-7), `threat-mitigations.ts` 1119 lines. Every tool change touches one file; audit surface is maximal. | file-size scan 2026-08-01; memory: R-7 deferred — **Done 2026-08-03** (Wave 3: three-way decomposition, see status below) |
| W-05 | HIGH | **No CI + broken commit hooks.** No UCH project workflow in `.github/`; husky commit-msg runs `npx commitlint` (not installed at root) and `bash` → WSL launcher (no distro). Every commit bypasses verification (`--no-verify`). Regressions are invisible between agent sessions. | memory (2026-07-31 delivery wave); `.husky/` contents |
| W-06 | MEDIUM | **Flaky / environment-dependent tests.** `ingester-injection.test.ts` contends on shared `os.tmpdir()`; neural-fs Windows path test is env-dependent; `signal-store.test.ts` + `secrets-store.test.ts` hardcode `/tmp` (broken on Windows). | memory (flakes documented); grep `/tmp` in tests |
| W-07 | MEDIUM | **State-location ambiguity.** Persistence paths are relative (`.uccp/persist`), resolved against CWD, not the workspace root from the manifest. Project `.uccp/` (ignored test artifacts) vs workspace `.uccp/` (real state, untracked). | `cli/index.ts:123`, `cli/uccp.ts:108`; root `.gitignore` |
| W-08 | MEDIUM | **Closed 92-member event-type union.** Adding one event type (Wave E: `suggestion`) required edits in event-bus + recorder map + nervous-system table + trace map. Brittle extension path for a type-safe bus. | `neural-event-bus.ts` union = 92 members; Wave E diff touched 4 files |
| W-09 | MEDIUM | **Manual boot/lifecycle fragility.** `CognitiveCore` constructs 20+ organs inline with hand-ordered wiring; no start ordering, no per-organ error isolation, no health for boot. A failing organ can take down the whole core. | `cognitive-core.ts` constructor (241-line class, 90% wiring) |
| W-10 | LOW | **Repo hygiene.** `probe-pkg.ts` scratch at project root; `UCCP-persist-load-SUMMARY.md`, `hook-test-file.txt` at workspace root; importing `dist/cli/index.js` **side-effects: starts the MCP server** (observed live). Legacy `SPEC.md` (COS-era) still promoted in README. | listing + live smoke test; README.md:149 |
| W-11 | LOW | **Stale engine declaration.** `engines: node >=18` but vitest 4 + eslint 10 require Node 20+; env runs Node 24. | `package.json:24` |

## The powerup register (dependencies unlocked by fixes)

| # | Opportunity | Unlocked by |
|---|---|---|
| O-1 | UER P2: real ledger ingestion + cross-host exchange (IDEA-0047) | W-01 |
| O-2 | Observatory live view over persisted traces | W-01 |
| O-3 | Cross-session learning-loop continuity (taste/connectome survive restarts) | W-02 |
| O-4 | CVM reference implementation (IDEA-0045, unblocked by RFC-0004) | independent |
| O-5 | Trustable wave flow: CI gates + real commit hooks protect every future wave | W-05 |

## Wave plan (ordered)

1. **Wave 1 — Persistence write-path (W-01)** + Wave E landing (W-03) + light hygiene (W-10)
2. **Wave 2 — Organism persistence registry (W-02)**
3. **Wave 3 — Interface decomposition (W-04)**
4. **Wave 4 — CI + commit hooks (W-05)**
5. **Wave 5 — Test isolation & path portability (W-06)**
6. **Wave 6 — State-location contract + boot lifecycle (W-07, W-09)**
7. **Wave 7 — Extensible event surface (W-08)**
8. **Wave 8 — Powerup: UER P2 + observatory + cross-session learning (O-1…O-3)**

## Wave 1 detail (executing now)

- `TraceLedger`: optional `onAppend` sink fired in `append()`.
- `TraceRecorder`: `onTrace` option passed through to the ledger.
- `CognitiveCore` + `cli/uccp.ts`: wire `onTrace → persistence.append`.
- `TracePersistence`: re-entrancy guard so `loadInto` never re-writes loaded traces.
- New end-to-end test: recorder → sink → file → reload → ledger contains spans (and no self-duplication on boot).
- Gates: scoped tests + full suite + tsc + eslint.

## Wave 3 status (2026-08-03 — W-04 interface decomposition done)

Three monoliths decomposed into one-module-per-concern layouts. Public APIs and
test surfaces unchanged; all imports re-pointed; zero behavior drift.

| Target | Before | After |
|---|---|---|
| `src/kernel/cic/threat-mitigations.ts` (1273 ln, 14 classes + engine) | one file | `src/kernel/cic/mitigations/` — `t01.ts`…`t14.ts` (one module per threat, 58–114 ln), `engine.ts` (134 ln), `types.ts`, `index.ts`; `cic/index.ts` re-exports `export *` |
| `src/mcp/stdio-server.ts` (1315 ln, 3 registration blocks) | one file | shell (275 ln: JSON-RPC dispatch, resources, prompts) + `src/mcp/tools/` — `core-tools.ts` (367), `framework-tools.ts` (579), `package-tools.ts` (124), `types.ts` (`Tool`/`ToolRegistrar`/`MCPToolContext`) |
| `src/cli/index.ts` (1001 ln, R-7 debt) | one file | dispatch shell (138 ln: `COMMANDS` table + context factory) + `src/cli/` command modules: `server-commands`, `memory-commands`, `skill-commands`, `governance-commands`, `framework-commands`, `package-command`, `cir-command`, `productivity-commands`, `profile-command`, `context.ts` (shared `CliContext` + flag helpers), `help.ts` |

Net: −3,349 lines in three tracked files; +34 small focused modules.

**Gates 2026-08-03**: scoped vitest 89/89 (threat-mitigations-all + 6 mcp-* suites +
e2e-server-flow), full suite **215 files / 3098 tests passed, 0 failures** (3
skipped), `tsc --noEmit` — zero errors in W-04 files (9 errors remain, all in
parallel-wave untracked files: `cells/`, `src/cells/`, middleware wave's
in-flight `exoskeleton.ts`), `eslint` exit 0 on all 29 W-04 files.

Still open: W-01/02 (persistence — landed as waves 1–2, see git log 6dde40b /
e634f76), W-05 (CI — b49ef33), W-06 remainder (ingester flake, env-dependent),
W-07/09 (a5fc805), W-08 (extensible event surface).

## Wave 0 status (2026-08-01 — tracked-clean items landed, uncommitted)

Executed without touching parallel-wave-owned files (persistence, CVM, entity,
belief, CAL, UER-ingest, repo-indexer waves all active):

| Item | Action | Evidence |
|---|---|---|
| W-06 (partial) | `signal-store.test.ts`, `secrets-store.test.ts`, `plugins.test.ts`, `sse-server.test.ts`, `expanded-features.test.ts`: hardcoded `/tmp` → `os.tmpdir()`-rooted unique paths (no more drive-root pollution on Windows) | scoped vitest 135/135 green; eslint clean on all 5 |
| W-10 | `probe-pkg.ts` scratch removed (`git rm`) | file was tracked-only, zero references |
| W-11 | `package.json` engines `>=18` → `>=20` (vitest 4 + eslint 10 require Node 20+; env runs Node 24) | `npm` accepts; tsc/eslint unaffected |

Still open (blocked by active waves): W-01/02 (persistence wave), W-04/07/09
(interface + core waves), W-08 (event surface), W-06 remainder (ingester flake
under parallel load, env-dependent — do not chase). Full-suite failures
2026-08-01 17:31Z = 3 tests in parallel untracked files only (uer-ingest ×2,
universal-entity ×1).

## Verification gates (every wave)

```text
npx vitest run            # full suite green
npx tsc --noEmit          # exit 0
npx eslint src/           # exit 0
node scripts/spec-version-check.mjs   # exit 0 (if spec/ touched)
```
