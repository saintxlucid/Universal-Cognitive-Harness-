---
phase: 01-organism-organs
plan: 02
type: execute
wave: 1
depends_on: [01-01]
files_modified:
  - src/workspace-graphs/knowledge-graph.ts
  - src/workspace-graphs/decision-graph.ts
  - src/workspace-graphs/task-graph.ts
  - src/workspace-graphs/evolution-history.ts
  - src/workspace-graphs/workspace-dna.ts
  - src/workspace-graphs/index.ts
autonomous: true
requirements: [D-01, D-02, D-05, D-07]
user_setup: []

must_haves:
  truths:
    - "All 5 organs exist as a cohesive src/workspace-graphs/ subsystem (D-01) with zero new runtime dependencies (D-02, D-07)"
    - "All 4 graph organs are GraphStore-backed with per-organ SQLite at {basePath}/{slug}/graph.sqlite; WorkspaceDNA is a plain JSON Storable (D-02/D-05)"
    - "All 5 organs implement Storable persist/load with the locked manifest semantics (D-05)"
    - "The 5 Plan-01-01 test files pass GREEN — contracts implemented exactly as specified, no renegotiation"
    - "Deterministic ids everywhere; no crypto.randomUUID(); no duplication of DecisionLog/TaskScheduler data; no imports from workspace-brain (import-direction lock, no circulars)"
  artifacts:
    - path: "src/workspace-graphs/knowledge-graph.ts"
      provides: "WorkspaceKnowledgeGraph class (implements Storable) — artifact/commit/build/failure/pr/review ingestion, touches edges, queries, manifest persist/load"
      exports: ["WorkspaceKnowledgeGraph"]
    - path: "src/workspace-graphs/decision-graph.ts"
      provides: "WorkspaceDecisionGraph class + DecisionRelation type — decision nodes (empty properties), causes/alternative_to/supersedes/references edges"
      exports: ["WorkspaceDecisionGraph", "DecisionRelation"]
    - path: "src/workspace-graphs/task-graph.ts"
      provides: "WorkspaceTaskGraph class — task nodes, depends_on/status_changed/touches/informed_by edges"
      exports: ["WorkspaceTaskGraph"]
    - path: "src/workspace-graphs/evolution-history.ts"
      provides: "WorkspaceEvolutionHistory class + CycleSummary — cycle nodes, counters, manifest persist/load with counter restore"
      exports: ["WorkspaceEvolutionHistory", "CycleSummary"]
    - path: "src/workspace-graphs/workspace-dna.ts"
      provides: "WorkspaceDNA class + DnaMutation — sha256 fingerprint, mutation tracking, JSON persist/load"
      exports: ["WorkspaceDNA", "DnaMutation"]
    - path: "src/workspace-graphs/index.ts"
      provides: "Subsystem barrel exporting all 5 organs + types (matches src/kernel/index.ts precedent)"
      exports: ["WorkspaceKnowledgeGraph", "WorkspaceDecisionGraph", "WorkspaceTaskGraph", "WorkspaceEvolutionHistory", "WorkspaceDNA"]
  key_links:
    - from: "src/workspace-graphs/*.ts"
      to: "src/kernel/storage/graph-store.ts"
      via: "value import (GraphStore) — allowed direction workspace-graphs → kernel/*"
      pattern: "import \\{ GraphStore \\} from '../kernel/storage/graph-store\\.js'"
    - from: "src/workspace-graphs/*.ts"
      to: "src/cognitive-plane/persistence/persistence-engine.ts"
      via: "value import (writeSnapshot, readSnapshot, type Storable)"
      pattern: "persistence-engine\\.js"
    - from: "src/workspace-graphs/task-graph.ts"
      to: "src/cognitive-plane/scheduler/task-scheduler.ts"
      via: "TYPE-ONLY import (TaskStatus) — no value import"
      pattern: "import type \\{ TaskStatus \\}"
    - from: "src/workspace-graphs/workspace-dna.ts"
      to: "node:crypto"
      via: "createHash('sha256')"
      pattern: "createHash\\('sha256'\\)"
    - from: "src/__tests__/workspace-graphs-*.test.ts"
      to: "src/workspace-graphs/*.ts"
      via: "ESM imports written in Plan 01-01 — implementations must match those exact names"
      pattern: "from '../workspace-graphs/"
---

<objective>
Implement the five organism organs (GREEN phase for Plan 01-01's RED scaffolds): WorkspaceKnowledgeGraph, WorkspaceDecisionGraph, WorkspaceTaskGraph, WorkspaceEvolutionHistory, WorkspaceDNA in the new cohesive `src/workspace-graphs/` subsystem (D-01), reusing GraphStore + Storable foundations (D-02), with the locked persist/load manifest semantics (D-05). Zero new runtime dependencies (D-02/D-07). No import from `workspace-brain` (circular-import lock).

Purpose: Turn the wave-0 test contracts into working, type-checked production code. The tests from Plan 01-01 are the acceptance contract — implement exactly to them.

Output: 6 new source files in `src/workspace-graphs/`; all 5 organ test files green; `npm run typecheck` clean.
</objective>

<execution_context>
@X:/Software Development/.opencode/get-shit-done/workflows/execute-plan.md
@X:/Software Development/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@X:/Software Development/projects/uch/planning/01-organism-organs/01-CONTEXT.md
@X:/Software Development/projects/uch/planning/01-organism-organs/01-RESEARCH.md
@X:/Software Development/projects/uch/planning/01-organism-organs/01-01-PLAN.md (the &lt;interfaces&gt; block IS the contract — implement exactly that surface)
@X:/Software Development/projects/uch/src/kernel/storage/graph-store.ts
@X:/Software Development/projects/uch/src/cognitive-plane/persistence/persistence-engine.ts
@X:/Software Development/projects/uch/src/cortex_kernel/reason-graph.ts (wrapper-pattern reference)

<interfaces>
The contracts are locked in Plan 01-01's &lt;interfaces&gt; block (identical tables for all 5 organs). Read that file FIRST. Key extracted facts for implementation:

From src/kernel/storage/graph-store.ts:
```typescript
export interface GraphNode { id: string; type: string; name: string; properties: Record<string, unknown>; created_at: number; }
export interface GraphEdge { id: string; source: string; target: string; relationship: string; properties: Record<string, unknown>; weight: number; valid_at: number; invalid_at: number | null; created_at: number; }
export class GraphStore {
  constructor(basePath: string);               // mkdirSync recursive + DatabaseSync at join(basePath, 'graph.sqlite')
  addNode(id: string, type: string, name: string, properties?: Record<string, unknown>): void;  // INSERT OR REPLACE
  getNode(id: string): GraphNode | null;
  findNodesByType(type: string): GraphNode[];
  searchNodes(query: string): GraphNode[];      // LIKE on name OR properties
  addEdge(id: string, source: string, target: string, relationship: string, properties?: Record<string, unknown>, weight?: number): void; // INSERT OR REPLACE
  getEdgesBetween(source: string, target: string): GraphEdge[];
  getEdgesFrom(nodeId: string, onlyValid?: boolean): GraphEdge[];  // edges where node is source OR target
  bfs(startId: string, maxDepth?: number): GraphNode[][];
  findPaths(from: string, to: string, maxDepth?: number): { nodes: GraphNode[]; edges: GraphEdge[] }[];
  nodeCount(): number; edgeCount(): number; clear(): void; close(): void;
}
```

From src/cognitive-plane/persistence/persistence-engine.ts:
```typescript
export interface Storable { persist(filePath: string): Promise<void>; load(filePath: string): Promise<number>; }
export function writeSnapshot(filePath: string, data: unknown): void;   // mkdir-recursive + pretty JSON
export function readSnapshot<T>(filePath: string): T | null;            // null on missing file; dateReviver for ISO strings
```

From src/cortex_kernel/reason-graph.ts (provenance-bearing edge precedent): edge `properties` carry `{ confidence, evidence }`; this phase's contract fixes them as `{ provenance: { source, source_id, timestamp: number, reliability }, confidence: { value, method: 'process_reliability', calibration_history: [] } }` — timestamp is epoch-ms NUMBER (never the Date-typed `Provenance.timestamp` from provenance.ts — GraphStore properties JSON-serialize without a reviver, Pitfall 2).

Provenance factory (per-organ private helper): `{ provenance: { source: 'system_log', source_id: <caller-supplied>, timestamp: Date.now(), reliability: 1.0 }, confidence: { value: 1.0, method: 'process_reliability' as const, calibration_history: [] } }`.

From src/neural-fs/version-store.ts:58-68 (stableSerialize precedent — REIMPLEMENT locally in workspace-dna.ts, do NOT import from neural-fs):
```typescript
// sorted-key recursive JSON — copy semantics, not import
if (value === null || typeof value !== 'object') return JSON.stringify(value);
if (Array.isArray(value)) return `[${value.map(stableSerialize).join(',')}]`;
const keys = Object.keys(value).sort();
return `{${keys.map((k) => `${JSON.stringify(k)}:${stableSerialize((value as Record<string, unknown>)[k])}`).join(',')}}`;
```

Strictness: tsconfig has `strict` + `noUncheckedIndexedAccess` — every array index access needs `!` or `?.` (e.g., `message.split('\n')[0] ?? message`, `findNodesByType(...)[0]!`). `npm run typecheck` (tsc --noEmit, tests excluded) must pass.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement WorkspaceKnowledgeGraph + WorkspaceDecisionGraph</name>
  <files>src/workspace-graphs/knowledge-graph.ts, src/workspace-graphs/decision-graph.ts</files>
  <read_first>
    - src/workspace-graphs/../__tests__/workspace-graphs-knowledge.test.ts (the RED contract — every method/name/id asserted here MUST exist)
    - src/workspace-graphs/../__tests__/workspace-graphs-decisions.test.ts (the RED contract)
    - src/kernel/storage/graph-store.ts (full API, already read — see interfaces block)
  </read_first>
  <action>
    Create `src/workspace-graphs/knowledge-graph.ts` exporting `class WorkspaceKnowledgeGraph implements Storable` per the Plan 01-01 contract table:
    - Constructor `constructor(basePath: string, maxNodes = 10000)`: private `readonly graph = new GraphStore(join(basePath, 'knowledge-graph'))`; private `maxNodes`. Import: `import { GraphStore } from '../kernel/storage/graph-store.js'; import { writeSnapshot, readSnapshot, type Storable } from '../cognitive-plane/persistence/persistence-engine.js'; import { join, basename } from 'node:path';`
    - Cap guard (private method or inline in every node-creating method): `if (this.graph.nodeCount() >= this.maxNodes && !this.graph.getNode(id)) return;`
    - `recordArtifact(path: string, meta?: { language?: string; lines?: number })`: id `file:${path}`; addNode(id, 'artifact', basename(path), { path, language: meta?.language, lines: meta?.lines } filtered to defined keys — build the properties object conditionally so undefined fields are omitted (contract test asserts properties.path; keep object minimal).
    - `recordCommit(hash: string, message: string, files?: string[] = [])`: addNode(`commit:${hash}`, 'commit', message.split('\n')[0] ?? message, { hash, message }); for each file in files: recordArtifact(file) then addEdge(`commit:${hash}:touches:${file}`, `commit:${hash}`, `file:${file}`, 'touches', provenanceProperties(hash), 1.0).
    - `recordBuild(id)`: addNode(`build:${id}`, 'build', 'build', { id }).
    - `recordFailure(id, message, kind: 'test' | 'build' | 'error')`: addNode(`failure:${id}`, 'failure', 'failure', { message, kind }).
    - `recordPr(id, title, url)`: addNode(`pr:${id}`, 'pr', title, { title, url }).
    - `recordReview(id, prId)`: addNode(`review:${id}`, 'review', 'review', { prId }).
    - Queries: `findArtifacts()/findCommits()/findFailures()` → `this.graph.findNodesByType('artifact'|'commit'|'failure')`; `search(query)` → searchNodes; `bfs(startId, maxDepth = 3)`; `findPaths(from, to, maxDepth = 5)`; `getEdgesFrom(nodeId)` → `this.graph.getEdgesFrom(nodeId)` (contract requires it — tests assert touches-edge ids/relationships); `nodeCount()`; `edgeCount()`; `close()`.
    - Import `type GraphEdge` from graph-store.js for the getEdgesFrom return type.
    - `async persist(filePath)`: `writeSnapshot(filePath, { version: 1, organ: 'knowledge-graph', graphPath: 'graphs/knowledge-graph/graph.sqlite', nodeCount: this.graph.nodeCount(), edgeCount: this.graph.edgeCount(), savedAt: new Date() })`.
    - `async load(filePath)`: `const data = readSnapshot<{ version?: number }>(filePath); if (!data) return 0; return this.graph.nodeCount();`.
    - Private `provenanceProperties(sourceId: string)` helper returning the locked `{ provenance: { source: 'system_log', source_id: sourceId, timestamp: Date.now(), reliability: 1.0 }, confidence: { value: 1.0, method: 'process_reliability' as const, calibration_history: [] } }`.

    Create `src/workspace-graphs/decision-graph.ts` exporting `class WorkspaceDecisionGraph implements Storable` and `export type DecisionRelation = 'causes' | 'alternative_to' | 'supersedes' | 'references'`:
    - Constructor: GraphStore at `join(basePath, 'decision-graph')`, maxNodes default 10000, cap guard identical to Task 1.
    - `recordDecision(decisionId: string, title: string)`: addNode(`decision:${decisionId}`, 'decision', title, {}) — EMPTY properties object (contract test asserts `Object.keys(node.properties).length === 0` — do not add any field).
    - `recordRelation(fromDecisionId, toDecisionId, relation: DecisionRelation)`: addEdge(`${relation}:${fromDecisionId}:${toDecisionId}`, `decision:${fromDecisionId}`, `decision:${toDecisionId}`, relation, provenanceProperties(`${fromDecisionId}:${toDecisionId}`), 1.0).
    - `recordDecisionFile(decisionId, filePath)`: ensure artifact node `file:${filePath}` (type 'artifact', name basename(filePath), properties { path: filePath }) via the cap-guarded pattern; addEdge(`references:${decisionId}:${filePath}`, `decision:${decisionId}`, `file:${filePath}`, 'references', provenanceProperties(filePath), 1.0).
    - `getDecision(decisionId)` → `this.graph.getNode('decision:' + decisionId)`; `getRelations(decisionId)` → `this.graph.getEdgesFrom('decision:' + decisionId)`; `findPaths(from, to, maxDepth = 5)`; `nodeCount()`; `edgeCount()`; `close()`.
    - `persist(filePath)`: `{ version: 1, organ: 'decision-graph', graphPath: 'graphs/decision-graph/graph.sqlite', nodeCount, edgeCount, savedAt: new Date() }`; `load(filePath)`: null → 0 else `this.graph.nodeCount()`.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/workspace-graphs-knowledge.test.ts src/__tests__/workspace-graphs-decisions.test.ts</automated>
    — GREEN: all tests in both files pass (expected ~19 tests).
    <automated>npx tsc --noEmit src/workspace-graphs/knowledge-graph.ts src/workspace-graphs/decision-graph.ts --noEmitOnError --strict --module nodenext --moduleResolution nodenext --target es2022 --skipLibCheck</automated>
    — no type errors in the two new files (full-project `npm run typecheck` is gated after Task 3; this scoped tsc flags contract mismatches early). If the scoped tsc invocation is noisy about project config, fall back to `npm run typecheck` ONLY if the two files are the only workspace-graphs files present so far.
  </verify>
  <acceptance_criteria>
    Both classes exist with the exact method signatures from the contract; provenanceProperties helper used on all edges; deterministic ids; cap guard in every node-creating method; the two organ test files pass 100% green.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement WorkspaceTaskGraph + WorkspaceEvolutionHistory</name>
  <files>src/workspace-graphs/task-graph.ts, src/workspace-graphs/evolution-history.ts</files>
  <read_first>
    - src/__tests__/workspace-graphs-tasks.test.ts (RED contract)
    - src/__tests__/workspace-graphs-evolution.test.ts (RED contract — note the counters-manifest assertion: `(JSON.parse(...)).counters.cycles`)
    - src/cognitive-plane/scheduler/task-scheduler.ts (TaskStatus type)
    - src/cognitive-plane/persistence/persistence-engine.ts (readSnapshot type param pattern from decision-log.ts:155-165)
  </read_first>
  <action>
    Create `src/workspace-graphs/task-graph.ts` exporting `class WorkspaceTaskGraph implements Storable`:
    - Imports: GraphStore; writeSnapshot/readSnapshot/Storable; `import type { TaskStatus } from '../cognitive-plane/scheduler/task-scheduler.js';`; `join, basename` from node:path.
    - Constructor: GraphStore at `join(basePath, 'task-graph')`, maxNodes 10000, cap guard.
    - `recordTask(taskId, name)`: addNode(`task:${taskId}`, 'task', name, {}) — EMPTY properties (contract asserts zero keys).
    - `recordDependency(taskId, dependsOnId)`: addEdge(`dep:${taskId}:${dependsOnId}`, `task:${taskId}`, `task:${dependsOnId}`, 'depends_on', provenanceProperties(`${taskId}:${dependsOnId}`), 1.0).
    - `recordStatusChange(taskId, from: TaskStatus, to: TaskStatus)`: addEdge(`status:${taskId}:${from}:${to}`, `task:${taskId}`, `task:${taskId}`, 'status_changed', { ...provenanceProperties(taskId), from, to }, 1.0) — SELF-EDGE (source === target === task node; contract test asserts this).
    - `recordTaskFile(taskId, filePath)`: cap-guarded artifact node `file:${filePath}` (type 'artifact', name basename, properties { path: filePath }); addEdge(`touches:${taskId}:${filePath}`, `task:${taskId}`, `file:${filePath}`, 'touches', provenanceProperties(filePath), 1.0).
    - `recordTaskDecision(taskId, decisionId)`: addEdge ONLY — `informed_by:${taskId}:${decisionId}` rel 'informed_by' from `task:${taskId}` to `decision:${decisionId}`; DO NOT create any `decision:` node (contract test asserts nodeCount unchanged).
    - Queries: `getTask(taskId)` → getNode(`task:${taskId}`); `getDependencies(taskId)` → `getEdgesFrom('task:' + taskId).filter(e => e.relationship === 'depends_on')`; `getStatusTransitions(taskId)` → `.filter(e => e.relationship === 'status_changed')`; `findPaths(from, to, maxDepth = 5)`; `nodeCount()`; `edgeCount()`; `close()`.
    - `persist`: `{ version: 1, organ: 'task-graph', graphPath: 'graphs/task-graph/graph.sqlite', nodeCount, edgeCount, savedAt: new Date() }`; `load`: null → 0 else `graph.nodeCount()`.

    Create `src/workspace-graphs/evolution-history.ts` exporting `interface CycleSummary` (all timestamps epoch-ms NUMBERS — contract shape) and `class WorkspaceEvolutionHistory implements Storable`:
    - Constructor: GraphStore at `join(basePath, 'evolution-history')`; `maxCycles = 10000`; private counters `{ cycles: 0, successful: 0, failed: 0, mutationsKept: 0 }`.
    - `recordCycle(summary: CycleSummary)`: FIRST the counter cap guard — `if (this.counters.cycles >= this.maxCycles) return;` — then addNode(`cycle:${summary.cycleId}`, 'cycle', `Cycle ${summary.cycleId}`, { startedAt: summary.startedAt, completedAt: summary.completedAt, durationMs: summary.durationMs, successful: summary.successful, improvements: summary.improvements, mutationsApplied: summary.mutationsApplied, mutationsKept: summary.mutationsKept }); then update counters: `this.counters.cycles++; summary.successful ? this.counters.successful++ : this.counters.failed++; this.counters.mutationsKept += summary.mutationsKept;`
    - `getCycles()` → `graph.findNodesByType('cycle')` (insertion order is SQLite rowid order — matches contract expectation); `getCycle(cycleId)` → `graph.getNode('cycle:' + cycleId)`; `getCycleCount()` → `this.counters.cycles`; `getStats()` → `{ ...this.counters }` shaped `{ cycles, successful, failed, mutationsKept }`.
    - `nodeCount()`; `edgeCount()`; `close()`.
    - `async persist(filePath)`: `writeSnapshot(filePath, { version: 1, organ: 'evolution-history', graphPath: 'graphs/evolution-history/graph.sqlite', counters: { ...this.counters }, nodeCount: this.graph.nodeCount(), edgeCount: this.graph.edgeCount(), savedAt: new Date() })`.
    - `async load(filePath)`: `const data = readSnapshot<{ counters?: { cycles?: number; successful?: number; failed?: number; mutationsKept?: number } }>(filePath); if (!data) return 0; if (data.counters) { this.counters.cycles = data.counters.cycles ?? 0; this.counters.successful = data.counters.successful ?? 0; this.counters.failed = data.counters.failed ?? 0; this.counters.mutationsKept = data.counters.mutationsKept ?? 0; } return this.counters.cycles;`
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/workspace-graphs-tasks.test.ts src/__tests__/workspace-graphs-evolution.test.ts</automated>
    — GREEN: all tests pass (expected ~17 tests). The evolution counters-manifest test (JSON.parse counters.cycles === 2) MUST pass — if it fails, the manifest shape differs from the contract; fix the implementation, not the test.
  </verify>
  <acceptance_criteria>
    Both classes exist per contract; recordStatusChange produces self-edges with id `status:${taskId}:${from}:${to}`; recordTaskDecision creates edges only; evolution counters updated per cycle and restored by load; both test files green.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Implement WorkspaceDNA + subsystem barrel; typecheck gate</name>
  <files>src/workspace-graphs/workspace-dna.ts, src/workspace-graphs/index.ts</files>
  <read_first>
    - src/__tests__/workspace-graphs-dna.test.ts (RED contract — fingerprint regex, key-order stability, no-secret-leakage cases)
    - src/neural-fs/version-store.ts lines 58-72 (stableSerialize + contentID precedent — REIMPLEMENT locally, do not import)
    - src/kernel/index.ts (barrel precedent — plain re-exports)
  </read_first>
  <action>
    Create `src/workspace-graphs/workspace-dna.ts` exporting `interface DnaMutation { at: number; locus: string; fromFingerprint: string | null; toFingerprint: string; cause: 'input_changed' | 'deliberate'; }` and `class WorkspaceDNA implements Storable`:
    - Imports: `import { createHash } from 'node:crypto'; import { writeSnapshot, readSnapshot, type Storable } from '../cognitive-plane/persistence/persistence-engine.js';` — NOTHING else (no workspace-brain, no neural-fs, no GraphStore).
    - Constructor `constructor(workspaceId: string, name: string, maxMutations = 1000)`: private `fingerprint: string | null = null`; private `mutations: DnaMutation[] = []`.
    - Private `stableSerialize(value: unknown): string` — local sorted-key recursive serializer, semantics exactly as version-store.ts:58-68 (see interfaces block; note `(value as Record<string, unknown>)[k]` needs a cast for strict TS).
    - `recompute(input: Record<string, unknown>): void`:
      ```
      const next = `sha256:${createHash('sha256').update(this.stableSerialize(input)).digest('hex')}`;
      if (next !== this.fingerprint && this.fingerprint !== null) {
        this.mutations.push({ at: Date.now(), locus: 'workspace', fromFingerprint: this.fingerprint, toFingerprint: next, cause: 'input_changed' });
        while (this.mutations.length > this.maxMutations) this.mutations.shift();
      }
      this.fingerprint = next;
      ```
    - `getFingerprint(): string | null`; `getMutations(): DnaMutation[]` (return copy `[...this.mutations]`); `mutationCount(): number`.
    - `async persist(filePath)`: `writeSnapshot(filePath, { workspaceId: this.workspaceId, name: this.name, fingerprint: this.fingerprint, mutations: this.mutations })` (store workspaceId/name as private constructor fields).
    - `async load(filePath)`: `const data = readSnapshot<{ workspaceId?: string; name?: string; fingerprint?: string | null; mutations?: DnaMutation[] }>(filePath); if (!data) return 0; this.fingerprint = data.fingerprint ?? null; this.mutations = data.mutations ?? []; return this.mutations.length;`

    Create `src/workspace-graphs/index.ts` — subsystem barrel re-exporting all five organs and their types (plain re-export lines, no logic):
    `export { WorkspaceKnowledgeGraph } from './knowledge-graph.js';`
    `export { WorkspaceDecisionGraph, type DecisionRelation } from './decision-graph.js';`
    `export { WorkspaceTaskGraph } from './task-graph.js';`
    `export { WorkspaceEvolutionHistory, type CycleSummary } from './evolution-history.js';`
    `export { WorkspaceDNA, type DnaMutation } from './workspace-dna.js';`

    GATE — full-project checks after all 6 files exist:
    1. `npm run typecheck` must be clean (strict + noUncheckedIndexedAccess — watch for `x[0]` index access in organ source; use `!`/`??` per codebase convention).
    2. `npx vitest run src/__tests__/workspace-graphs-knowledge.test.ts src/__tests__/workspace-graphs-decisions.test.ts src/__tests__/workspace-graphs-tasks.test.ts src/__tests__/workspace-graphs-evolution.test.ts src/__tests__/workspace-graphs-dna.test.ts` — ALL GREEN.
    3. Optional (not a gate): `npm run test:coverage` — new organs must not push project coverage below vitest.config.ts thresholds (statements 70 / branches 75 / functions 78 / lines 70).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/workspace-graphs-*.test.ts</automated>
    — GREEN: all 5 organ test files pass (expected ~35 tests total across the subsystem).
    <automated>npm run typecheck</automated>
    — exit 0, zero errors.
    <automated>Select-String -Path src/workspace-graphs/index.ts -Pattern "^export \{"</automated>
    — returns exactly 5 export lines (barrel completeness: 5 organs, plus the 3 `type` exports appear on the same lines).
  </verify>
  <acceptance_criteria>
    WorkspaceDNA implements the sha256 fingerprint + mutation-tracking contract with local stableSerialize; index.ts exports all 5 organs + 3 types; all 5 organ test files green; `npm run typecheck` exit 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| organ public methods ← callers (event handlers in Plan 01-03, tests) | Node ids derived from caller-supplied strings (paths, hashes, ids) — treated as opaque keys |
| GraphStore properties → SQLite JSON | Properties are JSON-stringified without a reviver; only JSON-safe values may cross |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | path-derived node ids (`file:${path}`) | mitigate | Organs NEVER resolve or read paths — ids are opaque SQLite keys; basename()/join() are pure string ops on the payload string; no fs access in any organ method |
| T-01-02 | Information Disclosure | graph properties / DNA fingerprint | mitigate | Property sets are whitelisted per method (contract tables); decision/task nodes carry EMPTY properties (no owner-store duplication); DNA hashes input via one-way sha256 — no input values (incl. potential secrets) are recoverable or stored in plaintext |
| T-01-03 | DoS | unbounded graph growth from repeated events | mitigate | Deterministic upsert ids (`file:${path}`, `commit:${hash}`, `decision:${id}`, `dep:${a}:${b}`...) dedupe by INSERT OR REPLACE; cap guards (`maxNodes`/`maxCycles`/`maxMutations`, defaults mirroring decision-log.ts:34's maxEntries pattern) skip insertion past the cap |
| T-01-04 | Tampering | SQLite file integrity | mitigate | Per-organ GraphStore (isolated `{slug}/graph.sqlite`, never shared); organs expose close(); tests close before rmSync; graph bodies self-restore from SQLite on reopen |
| T-01-05 | Spoofing | provenance source labeling | accept | `source: 'system_log'` is a fixed label; local single-user system; matches ReasonGraph precedent (reason-graph.ts:34-40) |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies (D-02); only node: built-ins + in-repo modules imported; RESEARCH.md Package Legitimacy Audit confirms no install surface |

**Blocking threats:** none — all dispositions are mitigate (implemented via the locked contract) or accept (low-risk local posture).
</threat_model>

<verification>
- Per-task: the two named organ test files must be GREEN after each task (sampling rule from 01-VALIDATION.md: per-task commit → per-organ quick command).
- Task 3 gate: full subsystem test run + `npm run typecheck` (strict + noUncheckedIndexedAccess) exit 0.
- After wave 1: `npx vitest run src/__tests__/workspace-graphs-*.test.ts` green (~35 tests). Full `npm test` is gated at Plan 01-03 (brain wiring) — the workspace-brain.test.ts extension is still RED until then, so the full suite is intentionally not green at this wave.
- Import-direction audit (grep gate, must be zero matches): `Select-String -Path src/workspace-graphs\*.ts -Pattern "workspace-brain"` → no output (no workspace-graphs → workspace-brain imports; circular-import lock).
- `crypto.randomUUID` audit: `Select-String -Path src/workspace-graphs\*.ts -Pattern "randomUUID"` → no output.
</verification>

<success_criteria>
- 5 organ classes + barrel exist in `src/workspace-graphs/` implementing the Plan 01-01 contract exactly (no method/name/id drift — the tests prove it).
- All 5 organ test files green; `npm run typecheck` clean; zero workspace-graphs → workspace-brain imports; zero crypto.randomUUID in organ source.
- Zero new runtime dependencies (package.json untouched).
- No data duplication: decision/task node properties are empty objects; no DecisionLog/TaskScheduler/EvolutionEngine state copied into graphs.
</success_criteria>

<output>
Create `X:/Software Development/projects/uch/planning/01-organism-organs/01-02-SUMMARY.md` when done
</output>
