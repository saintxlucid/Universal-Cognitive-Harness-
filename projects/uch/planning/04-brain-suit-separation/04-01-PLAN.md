---
phase: 04-brain-suit-separation
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - src/__tests__/cognitive-core.test.ts
autonomous: true
requirements: [D-01, D-02, D-03, D-05, D-08]
user_setup: []

must_haves:
  truths:
    - "cognitive-core.test.ts exists in src/__tests__/ with deterministic, LLM-free vitest tests (D-08) encoding the locked CognitiveCore contracts (D-01/D-02)"
    - "The suite constructs CognitiveCore with NO llm/agentic/drivers config and asserts the organ surface + aether lifecycle + stats shapes (D-01/D-05)"
    - "The import-firewall test statically scans src/cognitive-core/*.ts sources for forbidden import prefixes (../llm/, ../agentic/, ../drivers/, ../suit/, ../exoskeleton/) and asserts zero matches (D-02)"
    - "Tests use the temp-dir lifecycle (mkdtempSync + close + rmSync) with zero filesystem residue; no test calls a provider API (D-08)"
  artifacts:
    - path: "src/__tests__/cognitive-core.test.ts"
      provides: "CognitiveCore contracts (D-01/D-02/D-05/D-08): standalone construction without LLM, organ member surface, start/stop lifecycle, stats/state shapes, aether tick registration, import firewall"
      contains: "describe('CognitiveCore"
  key_links:
    - from: "cognitive-core.test.ts"
      to: "src/cognitive-core/cognitive-core.ts"
      via: "ESM import (../cognitive-core/cognitive-core.js) — intentionally RED at wave 0 (module doesn't exist yet)"
      pattern: "cognitive-core/cognitive-core"
    - from: "cognitive-core.test.ts"
      to: "src/cognitive-core/index.ts"
      via: "ESM import (../cognitive-core/index.js) — RED at wave 0 (export surface doesn't exist yet)"
      pattern: "cognitive-core/index"
---

<objective>
Write the failing test scaffold (Wave 0, RED phase) for the Phase 04 Cognitive Core: `CognitiveCore` in `src/cognitive-core/` — the persistent brain subsystem that owns every cognitive organ, the trace ledger, and the aether loop with ZERO LLM/driver/suit imports (per 04-CONTEXT D-01/D-02).

Purpose: Lock the core contracts (construction without an LLM, the full organ member surface, start/stop lifecycle ordering, stats/state shapes, aether subsystem registrations, the import firewall) as executable expectations BEFORE any implementation exists (per 04-CONTEXT D-08). These tests are the acceptance contract that Plan 04-02 implements against.

Output: 1 test file in src/__tests__/ — expected to FAIL at wave 0 (RED: `src/cognitive-core/` does not exist; imports of `cognitive-core/cognitive-core.js` fail at collection).
</objective>

<execution_context>
@X:/Software Development/.opencode/get-shit-done/workflows/execute-plan.md
@X:/Software Development/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@X:/Software Development/projects/uch/planning/04-brain-suit-separation/04-CONTEXT.md
@X:/Software Development/projects/uch/src/exoskeleton/exoskeleton.ts (the god object being split — constructor lines 123-218, wiring 264-426, lifecycle 445-490, stats 492-529)
@X:/Software Development/projects/uch/src/aether/aether-core.ts (tick loop + register(name, {tick, status}) + getState/getStats shapes)
@X:/Software Development/projects/uch/src/cognitive-plane/persistence/persistence-engine.ts (Storable conventions)
@X:/Software Development/projects/uch/src/__tests__/connectome-expansion.test.ts (exoskeleton construction + temp-dir lifecycle + afterEach stop-then-rmSync)
@X:/Software Development/projects/uch/src/__tests__/exoskeleton-organs.test.ts (organ exercise conventions)
@X:/Software Development/projects/uch/src/__tests__/workspace-graphs-knowledge.test.ts (temp-dir lifecycle conventions)

<interfaces>
The test file you write MUST assert against these LOCKED contracts (identical in Plan 04-02). Do not invent alternate member names, shapes, or semantics — the implementation plan implements exactly this surface.

## Shared rules
- The core is constructed with NO LLM, NO drivers, NO agentic config: `new CognitiveCore({ workspaceId, workspaceName, workspaceRoot })` (D-01). No provider API anywhere (D-08).
- All asserts are synchronous where the member is synchronous (organ getters, getStats, getState). The ONLY async members on the core are `start()` and `stop()`.
- Tests use the temp-dir lifecycle: `let dir`; beforeEach `mkdtempSync(join(tmpdir(), 'uch-core-'))`; afterEach `await core.stop()` (if started) then `rmSync(dir, { recursive: true, force: true })`.

## CognitiveCore — src/cognitive-core/cognitive-core.ts

```ts
export interface CognitiveCoreConfig {
  workspaceId: string;
  workspaceName: string;
  workspaceRoot: string;
  traceFile?: string;           // default '.uccp/traces.jsonl'
  aetherConfig?: Partial<AetherConfig>;
  agentId?: string;             // default 'cognitive-core'; kernel provenance id
  policies?: PolicyEngine;      // optional injection (suit mode) — D-03
  auth?: Auth;                  // optional injection (suit mode) — D-03
  reflexEngine?: ReflexEngine;  // optional injection (suit mode) — D-03
}
```

`class CognitiveCore` — organ members (ALL readonly, ALL exposed as public):

| Member | Type | Contract |
|---|---|---|
| `eventBus` | `NeuralEventBus` | bus instance the whole core shares |
| `nervousSystem` | `NervousSystem` | constructed with `{ trackEnergy: true }` |
| `metabolism` | `Metabolism` | component registrations include EXACTLY the six names: `exoskeleton`, `aether`, `endocrine`, `immune`, `sleep-cycle`, `reflex` |
| `consciousness` | `Consciousness` | live instance |
| `aether` | `AetherCore` | constructed with `{ tickIntervalMs: 5000, ...aetherConfig }`; `getSubsystems()` contains exactly `endocrine`, `immune`, `hippocampus` BEFORE start |
| `kernel` | `CognitiveKernel` | `agent_id` equals the config `agentId` (default `'cognitive-core'`) |
| `workspace` | `WorkspaceBrain` | constructed with `{ workspace_id, name, root_path, eventBus }` from config |
| `executive` | `ExecutiveBrain` | live instance |
| `traceRecorder` | `TraceRecorder` | live instance |
| `replay` | `CognitiveReplay` | wraps `traceRecorder.ledger` |
| `signals` | `SignalStore` | wraps `traceRecorder.ledger` |
| `persistence` | `TracePersistence` | constructed with `traceFile` (default `.uccp/traces.jsonl`) |
| `immuneSystem` | `ImmuneSystem` | live instance |
| `endocrineSystem` | `EndocrineSystem` | live instance |
| `sleepCycle` | `SleepCycle` | live instance |
| `actionSelector` | `ActionSelector` | live instance |
| `connectome` | `Connectome` | boot seeds wired (from `nervousSystem.emit` of `connectome:link` signals): after construction, `connectome.getConnections()` contains the seed edges `eventBus→traceRecorder` and `fsDriver→eventBus` |
| `hippocampus` | `Hippocampus` | live instance |
| `neocortex` | `Neocortex` | live instance |
| `cortexKernel` | `CortexKernel` | live instance |
| `policies` | `PolicyEngine` | injected instance when provided; fresh instance when absent |
| `auth` | `Auth` | injected instance when provided; fresh instance when absent |

Methods:

| Member | Contract |
|---|---|
| `async start()` | opens persistence + loads ledger into traceRecorder; starts aether; starts metabolism; emits `aether:started` on the event bus; observes a boot thought on aether |
| `async stop()` | stops metabolism; stops aether; closes persistence |
| `getState(): { running: boolean; startedAt: Date \| null; aetherPhase: string; subsystems: string[]; consciousness: ... }` | after `start()`: `running === true`, `startedAt` non-null, `subsystems` contains the aether registrations; after `stop()`: `running === false` |
| `getStats(): Record<string, unknown>` | object with keys `aether`, `nervousSystem`, `metabolism`, `endocrine`, `immune`, `sleep`, `connectome`, `actionSelector`, `hippocampus`, `neocortex` (same shapes as today's exoskeleton stats for those keys) |

## Import firewall (static, D-02)

```ts
const FORBIDDEN = ['../llm/', '../agentic/', '../drivers/', '../suit/', '../exoskeleton/'];
```
Read `src/cognitive-core/cognitive-core.ts` and `src/cognitive-core/index.ts` (await fs.readFile), assert `FORBIDDEN.every((p) => !source.includes(p))`.

## Type imports note
The test imports types (`AetherConfig`, `PolicyEngine`, `Auth`, `ReflexEngine`) ONLY for the `as` casts in the injection describe — use `import type` where possible so the firewall applies to runtime imports.
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: CognitiveCore test scaffold</name>
  <files>src/__tests__/cognitive-core.test.ts</files>
  <read_first>
    - src/__tests__/connectome-expansion.test.ts (exoskeleton construction, temp-dir lifecycle, afterEach stop-then-rmSync)
    - src/__tests__/exoskeleton-organs.test.ts (organ exercise conventions)
    - src/exoskeleton/exoskeleton.ts lines 123-218 (exact construction args being moved — copy them verbatim into the contracts)
    - src/exoskeleton/exoskeleton.ts lines 264-426 (registrations + wiring being moved)
    - src/exoskeleton/exoskeleton.ts lines 492-529 (getState/getStats shapes being moved)
    - src/aether/aether-core.ts (getSubsystems/getState/getStats signatures)
    - src/__tests__/workspace-graphs-knowledge.test.ts (temp-dir lifecycle conventions)
  </read_first>
  <action>
    Create `src/__tests__/cognitive-core.test.ts` implementing EXACTLY the contracts in the &lt;interfaces&gt; block above. ESM imports with `.js` extensions: `import { CognitiveCore } from '../cognitive-core/cognitive-core.js';` and `import type { CognitiveCoreConfig } from '../cognitive-core/cognitive-core.js';` plus `import { PolicyEngine } from '../control-plane/policies.js';`, `import { Auth } from '../control-plane/auth/auth.js';`, `import { ReflexEngine } from '../suit/instinct/reflex-engine.js';` (these EXIST today — only cognitive-core is RED).

    The file MUST contain `describe('CognitiveCore', ...)` with these cases:
    1. 'constructs without any LLM or driver configuration' — `new CognitiveCore({ workspaceId: 'ws-core', workspaceName: 'Core Test', workspaceRoot: dir })` succeeds; NO llm/agentic/drivers config in the constructor call.
    2. 'exposes the full organ surface' — typeof checks: eventBus, nervousSystem, metabolism, consciousness, aether, kernel, workspace, executive, traceRecorder, replay, signals, persistence, immuneSystem, endocrineSystem, sleepCycle, actionSelector, connectome, hippocampus, neocortex, cortexKernel, policies, auth all `toBeDefined()`.
    3. 'registers exactly the six metabolism components' — `core.metabolism.getComponentNames()` (or equivalent accessor from Metabolism's public API) deep-equals `['exoskeleton', 'aether', 'endocrine', 'immune', 'sleep-cycle', 'reflex']` (check the Metabolism public API in src/metabolism/metabolism.ts for the exact accessor; if none exists, use `getStats()` and assert the registered component names appear).
    4. 'registers endocrine, immune, hippocampus on the aether' — `core.aether.getSubsystems()` contains `'endocrine'`, `'immune'`, `'hippocampus'` BEFORE `start()`.
    5. 'uses the configured agentId as kernel provenance' — `new CognitiveCore({ ..., agentId: 'exoskeleton' })` → `core.kernel.getStats()` (or the equivalent accessor exposing agent_id) reflects `'exoskeleton'`; default construction reflects `'cognitive-core'`.
    6. 'wires the connectome boot seeds' — construction alone: `core.connectome.getConnections().length` ≥ 2; `core.connectome.getNodes()` contains `'eventBus'` and `'traceRecorder'`.
    7. 'accepts injected governance instances' — `const policies = new PolicyEngine(); const auth = new Auth(); const reflex = new ReflexEngine(); const core = new CognitiveCore({ ..., policies, auth, reflexEngine: reflex })` → `core.policies === policies`, `core.auth === auth`; `core.immuneSystem` is constructed (toBeDefined).
    8. 'creates fresh governance defaults when nothing is injected' — default construction → `core.policies` defined, `core.auth` defined (fresh instances; immune still constructed).
    9. 'starts and stops cleanly' — `await core.start()` → `core.getState().running === true`, `startedAt` non-null; `core.aether.getState().running === true`; `await core.stop()` → `getState().running === false`; `core.aether.getState().running === false`. (start/stop in the SAME test — no second start.)
    10. 'emits aether:started on start' — subscribe to `core.eventBus` (check NeuralEventBus public API for a subscribe/on method) BEFORE `start()`; after `start()`, received events contain type `'aether:started'`. Subscribe with a filter so no other event noise breaks the assertion; clean up the subscription in the test body.
    11. 'reports cognitive stats' — after `start()`+`stop()`: `getStats()` has keys `aether`, `nervousSystem`, `metabolism`, `endocrine`, `immune`, `sleep`, `connectome`, `actionSelector`, `hippocampus`, `neocortex`; `typeof stats.aether === 'object'`; `typeof stats.connectome === 'number'` (connection count, as in today's exoskeleton getStats).
    12. 'persists the trace file to the workspace root' — `new CognitiveCore({ ..., traceFile: join(dir, 'traces.jsonl') })` → `await core.start()`; `existsSync(join(dir, 'traces.jsonl'))` true; `await core.stop()`.
    13. 'enforces the import firewall' — separate `describe('Import firewall')`: `await fs.readFile(join(ROOT, 'src/cognitive-core/cognitive-core.ts'), 'utf8')` and `.../index.ts` (ROOT = path.resolve from the test file up to the repo root — compute via `fileURLToPath`/`dirname` or the known relative depth); assert `['../llm/', '../agentic/', '../drivers/', '../suit/', '../exoskeleton/'].every((p) => !source.includes(p))`.
    14. 'replay and signals share the trace ledger' — `core.replay` and `core.signals` both derive from `core.traceRecorder.ledger` (assert via their public state: `core.signals` expose the ledger reference through a documented accessor — if none exists, assert `typeof core.replay.getEvents() === 'function'` and `typeof core.signals.list() === 'function'` and that both return arrays after one start/stop cycle; keep the assertion to public API only).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/cognitive-core.test.ts</automated>
    Expected at wave 0: FAILS at collection — "Cannot find module '../cognitive-core/cognitive-core.js'" (RED state — modules are implemented in Plan 04-02). If it passes, the tests are too weak — they must reference the not-yet-existing modules.
    Secondary checks:
    - `Select-String -Path src/__tests__/cognitive-core.test.ts -Pattern "OPENAI_API_KEY|ANTHROPIC_API_KEY|new LLMClient"` → zero matches.
    - `Select-String -Path src/__tests__/cognitive-core.test.ts -Pattern "await core\.(remember|recall|plan)"` → zero matches (only start/stop are async on the core).
  </verify>
  <acceptance_criteria>
    cognitive-core.test.ts exists with describe('CognitiveCore') covering the 14 cases + the import-firewall describe; every case constructs the core with NO llm/agentic/drivers config; temp-dir lifecycle in beforeEach/afterEach; RED at collection; the firewall case statically scans the two core source files that Plan 04-02 will create.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| core → workspace root | Persistence writes (`traces.jsonl`) stay inside the injected workspaceRoot or explicit traceFile — tests always use mkdtemp dirs |
| test → core sources | The firewall test READS repo source files — read-only, no mutation |
| core → governance | Injected policies/auth are trusted suit-side objects; fresh defaults are permissive-local (matching today's dev-key posture) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | test residue | mitigate | temp-dir lifecycle: afterEach `await core.stop()` BEFORE `rmSync` (stop closes the trace file first); zero residue asserted by existing workspace conventions |
| T-04-02 | DoS | aether ticking in tests | mitigate | every started core is stopped in the SAME test or afterEach; no test leaves a ticking aether |
| T-04-03 | Information Disclosure | firewall reading sources | accept | local single-user runtime; reads only `src/cognitive-core/*.ts` |
| T-04-SC | Tampering | npm installs | accept | Zero installs this wave (D-02 Phase 02); no checkpoints required |

**Blocking threats:** none at wave 0 (test scaffold carries no production code paths; RED is bounded to the 1 named test file).
</threat_model>

<verification>
- Wave-0 gate is the RED state ONLY: `npx vitest run src/__tests__/cognitive-core.test.ts` fails at collection with "Cannot find module '../cognitive-core/cognitive-core.js'".
- Do NOT run the full `npm test` suite as a wave-0 gate — it is intentionally red until Plan 04-02 lands (full-suite runs gate waves after GREEN).
- Existing suites must remain untouched: `git status --porcelain src/` shows ONLY the 1 new test file.
</verification>

<success_criteria>
- cognitive-core.test.ts exists with describe('CognitiveCore') covering the 14 cases + import firewall.
- Runs RED at module collection; zero provider API usage; temp-dir lifecycle; zero residue.
- Contracts exactly match the &lt;interfaces&gt; block — Plan 04-02 implements against them without renegotiation.
- The core's "no LLM, no drivers" construction and the six metabolism component names are locked as executable assertions (the D-01/D-02 boundary).
</success_criteria>

<output>
Create `X:/Software Development/projects/uch/planning/04-brain-suit-separation/04-01-SUMMARY.md` when done
</output>
